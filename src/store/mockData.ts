import { Contact, Interaction, Hook, Connection } from '../types';

export const mockContacts: Record<string, Contact> = {};
export const mockInteractions: Record<string, Interaction> = {};
export const mockHooks: Record<string, Hook> = {};
export const mockConnections: Record<string, Connection> = {};
